var hazardousSubstanceInventory = function () {
    var selectors = {
        search: "#Search"
    };

    function initialise() {

        

    }

    return { initialise: initialise };
} ();

$(function () {
    hazardousSubstanceInventory.initialise();
});